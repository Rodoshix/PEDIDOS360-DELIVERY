spring:
  application:
    name: productos-service

  config:
    import: optional:file:.env[.properties]

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5439/pedidos360_productos}
    username: ${DB_USERNAME:pedidos360}
    password: ${DB_PASSWORD:replace-me}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        default_schema: productos
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: productos
    schemas: productos
    create-schemas: true
    clean-disabled: true

server:
  port: 8083
  address: 127.0.0.1

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always