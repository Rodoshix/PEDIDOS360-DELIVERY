services:
  postgres:
    image: postgres:17-alpine
    container_name: pedidos360-productos-postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: ${DB_NAME:-pedidos360_productos}
      POSTGRES_USER: ${DB_USERNAME:-pedidos360}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-replace-me}

    ports:
      - "${DB_PORT:-5439}:5432"

    volumes:
      - productos_postgres_data:/var/lib/postgresql/data

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${DB_USERNAME:-pedidos360} -d ${DB_NAME:-pedidos360_productos}"
        ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  productos_postgres_data: