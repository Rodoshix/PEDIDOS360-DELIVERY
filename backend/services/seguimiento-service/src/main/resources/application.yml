spring:
  config:
    import: optional:file:.env.local[.properties]
  application:
    name: seguimiento-service
  jackson:
    deserialization:
      fail-on-unknown-properties: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5434/pedidos360_seguimiento}
    username: ${DB_USERNAME:pedidos360_seguimiento}
    password: ${DB_PASSWORD:}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    locations: classpath:db/migration
    clean-disabled: true

server:
  port: ${SERVER_PORT:8088}
  address: ${SERVER_ADDRESS:127.0.0.1}

seguimiento:
  identidad-local:
    enabled: ${LOCAL_IDENTITY_ENABLED:false}
    tenant-id: ${LOCAL_TENANT_ID:}
    object-id: ${LOCAL_OBJECT_ID:}
    roles: ${LOCAL_ROLES:ADMIN}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
